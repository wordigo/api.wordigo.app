{
  "name": "wordigo_back",
  "private": true,
  "scripts": {
    "build": "turbo run build --filter=...api",
    "dev": "turbo dev --parallel --no-cache --continue --filter=...api",
    "start": "dotenv -- turbo run start --filter=...api",
    "lint": "turbo run lint",
    "clean": "git clean -xdf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:generate": "cd ./packages/db && npx prisma generate",
    "db:push": "cd ./packages/db && prisma db push --skip-generate"
  },
  "dependencies": {
    "turbo": "^1.10.11",
    "dotenv": "^16.3.1",
    "dotenv-cli": "^7.3.0"
  },
  "devDependencies": {
    "@turbo/gen": "^1.9.7",
    "eslint": "^7.32.0",
    "prettier": "^2.5.1"
  },
  "packageManager": "npm@9.5.0",
  "workspaces": [
    "apps/**",
    "packages/*"
  ]
}